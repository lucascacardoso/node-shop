form.mt-2(action="/add-to-cart" method="POST")
    input(type="hidden", name="productId", value=product._id)
    input(type="hidden", name="_csrf", value=csrfToken)
    button.btn.btn-success Add to Cart